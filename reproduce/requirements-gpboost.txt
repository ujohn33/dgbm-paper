# GPBoost baseline environment.
#
# GPBoost ran in its own Python 3.10 environment, which was not preserved, so
# this file is not a `pip freeze`. gpboost is pinned to 1.5.6, the release
# current when the GPBoost benchmark jobs ran (2025-03-22; the earlier UCI runs
# of 2024-11-07 used 1.5.3 from the same 1.5 series).
#
# 1.5.6 was verified to install and to expose exactly the API used by
# openml/openml_gpboost_HP_single_run.py and uci/UCI_gpboost_HP_single_run.py:
# GPModel(gp_coords=..., likelihood=..., gp_approx="vecchia"),
# set_optim_params({"optimizer_cov": "nelder_mead", "delta_rel_conv": ...}) and
# gpb.cv(..., train_gp_model_cov_pars=False).
#
#   python3.10 -m venv .venv-gpboost && source .venv-gpboost/bin/activate
#   pip install -r reproduce/requirements-gpboost.txt

gpboost==1.5.6
numpy>=1.22,<2
pandas>=1.4,<3
scipy>=1.8
scikit-learn>=1.1
optuna>=3.1,<4
openml==0.15.1
argon2-cffi
